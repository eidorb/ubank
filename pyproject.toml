[tool.poetry]
name = "ubank"
version = "1.0.0"
description = "Access ubank's API."
authors = ["eidorb"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
httpx = "^0.27.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
